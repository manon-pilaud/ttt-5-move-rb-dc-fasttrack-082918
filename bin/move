#!/usr/bin/env ruby

require_relative '../lib/move.rb'

# Code your CLI Here

board = Array.new(9," ")

puts "Welcome to Tic Tac Toe!"

user_input = gets.chomp
index = input_to_index(user_input)

move(board,index)
#can pass in "X" but redundant since their is a default X
display_board(board)